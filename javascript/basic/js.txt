call back=a function that is passed as an argument to another function
      used to handle asynchronous operation:
         1.reading file
         2.Network requests
         3.Interacting with database

 forEach() =method used to iterate over the elements of
            an array and apply a specified function(callback)
              to each elements

              array.forEach(callback)

  map()=accepts a callback and applies that function to
        each element of an array,then return 
  filter() =creates a new array by filtering out elements

  reduce() =reduce the element of an array to a single value


  function declaration =defines a reusableblock of code that performs a specific task

  function expression =a way to define function as value or variables

  //arrow function=a concise way to write the function expression good for simple functions that you use only once(parameter)=> some code                          